export const metadata = {
    title: "Dashboard",
    description: "Overview page for the dashboard section.",
};

const DashboardPage = () => {
    return (
        <div>
            <h2>This is Dashboard Page</h2>
        </div>
    );
};

export default DashboardPage;
