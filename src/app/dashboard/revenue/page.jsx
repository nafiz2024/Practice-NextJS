
export const metadata = {
    title: "Dashboard Revenue",
    description: "Revenue insights page inside the dashboard.",
};

const page = () => {
    return (
        <div>
            <h1>Money is coming soon</h1>
        </div>
    );
};

export default page;
